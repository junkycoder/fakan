{
  "name": "fakan-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor iOS shell pro fakan.cz",
  "scripts": {
    "build:web": "bash ../bin/build.sh",
    "sync": "npm run build:web && cap sync ios",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
